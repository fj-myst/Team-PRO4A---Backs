import 'package:flutter/material.dart';

class ViewerAdminHome extends StatelessWidget {
  const ViewerAdminHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Viewer Admin Home 👁️'),
      ),
    );
  }
}