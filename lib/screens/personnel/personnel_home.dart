import 'package:flutter/material.dart';

class PersonnelHome extends StatelessWidget {
  const PersonnelHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Personnel Home 👤'),
      ),
    );
  }
}